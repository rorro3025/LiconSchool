import { useState } from "react";

export default function Pokemon() {
  const [message, setMessage] = useState("");

  return (
    <div>
      <h1>API REST</h1>
        pokemones 
    </div>
  );
}
