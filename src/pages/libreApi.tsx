import Layout from "@/componets/Layout";
import { Grid, Button, Container } from '@mantine/core'

export default function LibreApi() {

    return (
        <Layout>
            <Container>
                <Button>Hello</Button>
            </Container>
        </Layout>
    )
}
